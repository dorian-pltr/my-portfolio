query GetProjects {
  project {
    id
    name
    description
    startDate
    endDate
    status {
      id
      name
    }
    technologies {
      id
      name
      isMain
      type {
        id
        name
      }
    }
    images {
      id
      url
      isMain
    }
  }
}
